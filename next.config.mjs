/** @type {import('next').NextConfig} */
const nextConfig = {
    // next.config.mjs
    images: {
        domains: ['assets.epicurious.com'], // Replace with the domain of the external image source
    },
}
export default nextConfig;
